This directory is intentionally excluded from the public branch contents.

Local and deployment-specific runtime artifacts such as ledgers, crypto material,
runbook stores, chaincode packages, and generated state must remain outside the
published snapshot.
